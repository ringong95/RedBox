/**
 * RedBox App!
 * https://github.com/facebook/react-native
 * @flow
 */

import { AppRegistry, } from 'react-native';
import RedBox from './js';

AppRegistry.registerComponent('redbox', () => RedBox);
